import {useSelector} from 'react-redux'

const Navbar = () => {
    return (
        <nav>
        <div className="nav-center">
            <h1>Redux Toolkit</h1>
            </div>
        </nav>
    )
}

export default Navbar;
